# PM Copilot Exoskeleton

AI-помощник для продактов и ресерчеров, работающий внутри SourceCraft Code Assistant (VS Code).

## Зачем

Типичный рабочий день PM — это десятки тикетов, разбросанные по очередям решения, обязательства из встреч и документы на вики. Собрать из этого целостную картину вручную тяжело. AI-ассистент может помочь, но внутренние модели (Qwen, Minimax) часто ошибаются в сложных задачах — путают инструменты, теряют контекст, выдают галлюцинации за факты.

PM Copilot решает эту проблему не за счёт ума модели, а за счёт структуры. Модель не импровизирует — она следует заранее проверенным рецептам, а данные проходят через детерминированный пайплайн с проверками.

## Что умеет

| Команда | Что делает |
|---------|-----------|
| `/brief` | Утренний брифинг: фокус недели, активные задачи, просроченные обязательства |
| `/my-work` | Подтягивает ваши тикеты из Трекера и обновляет рабочую память |
| `/issue PM-123` | Собирает досье на тикет: статус, контекст из вики, связанные документы |
| `/remember <факт>` | Записывает решение, договорённость или заметку в структурированную память |
| `/tool-calibrate` | Проверяет, что подключение к корпоративным инструментам работает |

## Как устроен

```
brain/
├── system/          ← операционная память ассистента (рецепты, алиасы, схемы)
├── ledger/          ← канонические данные (тикеты, решения, обязательства)
├── views/           ← человекочитаемые сводки (генерируются автоматически)
└── control/         ← ваш контекст: роль, цели квартала, фокус недели
```

**Принцип работы:** данные из Трекера и вики → нормализация → хранение с источником → генерация сводок. Если источник вернул пустой ответ — старые данные сохраняются. Если факт пришёл из ненадёжного источника — он помечается как кандидат и требует подтверждения.

## Быстрый старт

Подробная инструкция — в [SETUP.md](SETUP.md). Коротко:

1. Клонируйте репозиторий
2. Создайте `.venv` и установите зависимости
3. Скопируйте `.mcp.json.example` → `.mcp.json`
4. Заполните `brain/system/identities.yaml` своими данными
5. Запустите `/tool-calibrate` в Code Assistant
6. Готово — попробуйте `/my-work`

## Требования

- VS Code + SourceCraft Code Assistant
- Доступ к корпоративной сети
- Токен `~/.arc/token`
- Доступ через IDM к MCP-серверам: tracker, intrasearch, deepagent
